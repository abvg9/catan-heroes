package io.github.notaphplover.catan.core.game.handler;

import io.github.notaphplover.catan.core.request.IRequest;

public class MinimunRequestHandler extends RequestHandler<IRequest> {

  public MinimunRequestHandler(MinimunRequestHandlerBuilder builder) {
    super(builder);
  }
}
